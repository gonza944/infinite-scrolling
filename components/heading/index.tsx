import styles from './index.module.css'

const Heading = () => (
  <div className={styles.wrapper}>
    <h1 className={styles.header}>Infinite scroll app</h1>
    <h3 className={styles.subheading}>Browse beautiful images all day long!</h3>
  </div>
);

export default Heading;
